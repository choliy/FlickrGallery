<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:minHeight="@dimen/dialog_min_size"
    android:minWidth="@dimen/dialog_min_size"
    android:orientation="vertical"
    android:paddingBottom="@dimen/medium_margin"
    android:paddingTop="@dimen/medium_margin">

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginBottom="@dimen/material_margin"
        android:layout_marginStart="@dimen/medium_margin"
        android:text="@string/dialog_history_title"
        android:textColor="@color/textColorGray"
        android:textSize="@dimen/text_medium"
        android:textStyle="bold"/>

    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1">

        <android.support.v7.widget.RecyclerView
            android:id="@+id/rv_history"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:paddingEnd="@dimen/material_margin"
            android:paddingStart="@dimen/material_margin"
            android:scrollbars="vertical"/>

        <include layout="@layout/item_no_history"/>

    </FrameLayout>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="@dimen/material_margin"
        android:orientation="horizontal">

        <TextView
            android:id="@+id/btn_history_clear"
            style="@style/DialogButtonStyle"
            android:layout_marginStart="@dimen/medium_margin"
            android:paddingStart="@dimen/size_zero"
            android:text="@string/dialog_clear_btn"/>

        <View style="@style/ViewWeightStyle"/>

        <TextView
            android:id="@+id/btn_history_close"
            style="@style/DialogButtonStyle"
            android:layout_marginEnd="@dimen/medium_margin"
            android:paddingEnd="@dimen/size_zero"
            android:text="@string/dialog_close_btn"/>

    </LinearLayout>

</LinearLayout>